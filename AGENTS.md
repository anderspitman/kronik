After you make any changes to the native app, verify the build still works with
`make clean && STATIC_GLEW=1 make -j` before reporting back.

Backup the database to a new folder in ./backups before any changes.
